import { ObjetoAtextoPipe } from './objeto-atexto.pipe';

describe('ObjetoAtextoPipe', () => {
  it('create an instance', () => {
    const pipe = new ObjetoAtextoPipe();
    expect(pipe).toBeTruthy();
  });
});
